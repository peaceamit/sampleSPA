/*global define*/

define([
    'jquery',
    'backbone'
], function ($, Backbone) {
    'use strict';

    var PlayerRouter = Backbone.Router.extend({
        routes: {
        }

    });

    return PlayerRouter;
});
